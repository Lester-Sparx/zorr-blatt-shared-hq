# GITHUB SHARED HQ v0 — A01–A20 EVIDENCE MAP

This is a build-time evidence map, not a PASS/LOCK declaration.

| ID | Implementation evidence | Requires live GitHub inspection |
|---|---|---|
| A01 — CT SHA binding | pinned lock manifest + actual-byte verifier | immutable CT release |
| A02 — CT immutability | external artifact only; no CT source committed | release immutability |
| A03 — single authoritative HQ | state/tasks/records under protected `main` | branch ruleset |
| A04 — production values absent | only canonical JSON evidence is allowed; scope guard rejects operational values | no |
| A05 — artifact revision immutable | append-only artifact record + immutable release model | live release setting |
| A06 — SHA verification before QC | required `hq-artifact-sha` downloads and rehashes release | required-check ruleset |
| A07 — identities from GitHub auth | actor comes from authenticated Actions context | actual identities |
| A08 — one identity cannot hold multiple roles | registry rejects duplicate GitHub logins | actual role mapping |
| A09 — Lester self-QC impossible | actor-bound transition rejects builder QC | live PR attempt |
| A10 — old QC invalid on new revision | pointer and record bind exact revision | no |
| A11 — stale approval invalid after new commit | evidence binds candidate commit | stale-approval dismissal |
| A12 — CAS conflict blocks mutation | task revision + live base commit checked | up-to-date branch rule |
| A13 — QC PASS does not create LOCK | QC transition leaves `lockRecord` null | no |
| A14 — OWNER creates LOCK separately | trusted transition and proposal require OWNER | owner identity |
| A15 — LOCK binds artifact/QC/architecture hashes | canonical lock record verifies all evidence hashes | no |
| A16 — direct main push impossible | PR-only model | live ruleset |
| A17 — required checks cannot be bypassed | eight unique checks + protected-base validator | trusted check source/ruleset |
| A18 — G2 remains BLOCKED | state/schema/policy enforce G2 blocked | no |
| A19 — HQ does not write ZB CORE | no ZB CORE code/writer/path | live permissions review |
| A20 — dashboard is projection only | exact generated bytes; no state mutation | published page inspection |

No row requiring live GitHub inspection may be declared PASS from the local
archive alone.
