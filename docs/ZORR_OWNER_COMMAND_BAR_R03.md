# ZORR OWNER COMMAND BAR R03

STATUS = CANONICAL OWNER COMMAND SURFACE
BASE = REUSED ZORR OWNER COMMAND BAR R02 MV3 EXTENSION
LEGACY USERSCRIPT = SUPERSEDED / NOT CANONICAL

## Canonical package

- package: `ZORR_OWNER_COMMAND_BAR_R03.zip`
- SHA-256: `3c30f1cdc47d270030ea978c4bcdba8f6eb290b8e9dae8a1431df6edc97442a2`
- durable archive path: `/ZORR/PERMANENT_ARCHIVE_V1/TOOLS/OWNER_COMMAND_BAR/ZORR_OWNER_COMMAND_BAR_R03.zip`
- package verification before physical gate: `21/21 tests PASS`; `core.js`, `content.js`, `popup.js` syntax PASS; JSON parse PASS; ZIP integrity PASS.

## Base-first / reuse evidence

R03 is not a new toolbar implementation. It reuses the already working R02 Chrome/Edge Manifest V3 extension code path: `core.js`, `content.js`, popup/settings, draft safety, form-scoped send lookup, favorites/compact mode, storage hot-refresh, and SPA remount behavior. R03 only changes the canonical owner command registry/title/storage namespace required by ZORR MODE.

## Default compact commands

`ZORR MODE | ДЕЛАЙ | ПРОДОЛЖАЙ | ДЕЛАЙ ДО PASS | ПРОВЕРЬ`

The action commands embed `ZORR MODE` semantics so OWNER does not have to repeat study-first, reuse-first, no-treading-water, fresh-evidence, exact-HEAD, and truth laws manually.

Other R02 commands remain available through the full command view rather than becoming primary buttons.

## Physical evidence

On 2026-08-29, after the R03 replacement flow, OWNER sent `ZORR MODE` through the command surface and the receiving ChatGPT session executed a fresh GitHub/Constitution bootstrap. OWNER then explicitly returned `PASS` for the physical browser gate.

`OWNER_PC_PHYSICAL_PASS = YES`

This is an OWNER-authoritative physical/UI gate. It does not imply unrelated project components are production-active.

## One-critical-path law

The temporary `ZORR MODE Toolbar V1` Violentmonkey userscript is superseded and must not be treated as a fallback or second production path. The canonical OWNER command surface is R03 MV3 extension only.

## Authority

Execution behavior remains governed by current `main` `ZORR_EXECUTION_CONSTITUTION.md` and `AGENTS.md`. This command bar is only a shortcut surface; it cannot override project law or fabricate PASS/evidence.
