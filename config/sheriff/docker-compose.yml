name: zb-sheriff

services:
  forgejo:
    image: codeberg.org/forgejo/forgejo:15.0.7
    restart: unless-stopped
    environment:
      FORGEJO__database__DB_TYPE: sqlite3
      FORGEJO__database__PATH: /data/gitea/forgejo.db
    ports:
      - "3000:3000"
      - "2222:22"
    volumes:
      - forgejo-data:/data

  nats:
    image: nats:2.10-alpine
    restart: unless-stopped
    command: ["-c", "/etc/nats/nats.conf"]
    environment:
      NATS_PASSWORD: ${NATS_PASSWORD:-sheriff-dev-only}
    expose:
      - "4222"
      - "8222"
    volumes:
      # canonical repository source: config/sheriff/nats.conf
      - ./nats.conf:/etc/nats/nats.conf:ro
      - nats-data:/data/jetstream

  opa:
    image: openpolicyagent/opa:1.0.1-static
    restart: unless-stopped
    command: ["run", "--server", "--addr=0.0.0.0:8181", "/policy"]
    expose:
      - "8181"
    volumes:
      # canonical repository source: config/sheriff/opa
      - ./opa:/policy:ro

  postgres:
    image: postgres:17-alpine
    restart: unless-stopped
    environment:
      POSTGRES_DB: sheriff
      POSTGRES_USER: sheriff
      POSTGRES_PASSWORD: ${SHERIFF_DB_PASSWORD:-sheriff-dev-only}
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U sheriff -d sheriff"]
      interval: 5s
      timeout: 3s
      retries: 20
    expose:
      - "5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data
      # canonical repository source: config/sheriff/postgres
      - ./postgres:/docker-entrypoint-initdb.d:ro

  otel-collector:
    image: otel/opentelemetry-collector-contrib:0.117.0
    restart: unless-stopped
    command: ["--config=/etc/otelcol-contrib/config.yaml"]
    volumes:
      - ./otel-collector.yml:/etc/otelcol-contrib/config.yaml:ro
    expose:
      - "4317"
      - "4318"

  prometheus:
    image: prom/prometheus:v3.1.0
    restart: unless-stopped
    command: ["--config.file=/etc/prometheus/prometheus.yml"]
    volumes:
      - ./prometheus.yml:/etc/prometheus/prometheus.yml:ro
      - prometheus-data:/prometheus
    expose:
      - "9090"

  loki:
    image: grafana/loki:3.3.2
    restart: unless-stopped
    command: ["-config.file=/etc/loki/local-config.yaml"]
    volumes:
      - ./loki.yml:/etc/loki/local-config.yaml:ro
      - loki-data:/loki
    expose:
      - "3100"

  grafana:
    image: grafana/grafana-oss:11.4.0
    restart: unless-stopped
    environment:
      GF_SECURITY_ADMIN_PASSWORD: ${GRAFANA_ADMIN_PASSWORD:-admin-dev-only}
      SHERIFF_DB_PASSWORD: ${SHERIFF_DB_PASSWORD:-sheriff-dev-only}
    volumes:
      - grafana-data:/var/lib/grafana
      - ./grafana/provisioning:/etc/grafana/provisioning:ro
    ports:
      - "3001:3000"
    depends_on:
      - prometheus
      - loki

  sheriff-worker:
    build:
      context: ../..
      dockerfile: config/sheriff/Dockerfile.worker
    restart: unless-stopped
    environment:
      SHERIFF_NATS_URL: nats://sheriff:${NATS_PASSWORD:-sheriff-dev-only}@nats:4222
      SHERIFF_POSTGRES_DSN: postgresql://sheriff:${SHERIFF_DB_PASSWORD:-sheriff-dev-only}@postgres:5432/sheriff
      SHERIFF_OPA_URL: http://opa:8181/v1/data/zorr/sheriff/decision
      SHERIFF_METRICS_PORT: "9464"
      SHERIFF_EVENT_SCHEMA_PATH: /app/schemas/SHERIFF_AGENT_EVENT_V1.schema.json
    depends_on:
      nats:
        condition: service_started
      opa:
        condition: service_started
      postgres:
        condition: service_healthy
    expose:
      - "9464"

volumes:
  forgejo-data:
  nats-data:
  postgres-data:
  prometheus-data:
  loki-data:
  grafana-data:
