# ZB Local Agent Controller v0

Zero-budget Windows-local controller for fixed-contract ZORR BLATT agent tasks.

Mocked implementation phase only. Production model installation and live SALVADOR generation are intentionally out of scope until Tasks 1–6 are green.
