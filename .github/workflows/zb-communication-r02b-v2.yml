name: zb-communication-r02b-v2-retired

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-24.04
    steps:
      - name: Report retired R02B v2 execution route
        run: |
          echo "LEGACY_EXECUTION_ROUTE_RETIRED"
          echo "R02B v2 comment-triggered Copilot/Task execution is not an active ZORR route."
          echo "Issue #251 is the only product NEXT router."
