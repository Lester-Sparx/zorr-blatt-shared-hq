name: zb-r03-production-router-retired

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-24.04
    steps:
      - name: Report retired legacy path
        run: |
          echo "R03_RETIRED_FROM_AUTOMATION"
          echo "DUNCAN/ChatGPT is the orchestrator; GitHub Actions is deterministic verification only."
